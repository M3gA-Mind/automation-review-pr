# PR #2249 — channels: telegram remote-control phase 1 (status, sessions, new)
- **Author**: @CodeGhost21
- **Branch**: `cursor/a01-1805-telegram-remote-control-phase1` → `main`
- **Created**: 2026-05-20
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2249
- **Status**: changes-requested
- **Last reviewed commit**: fb1774c60f7334b354800a0360a9baa346ca1531
- **Last review date**: 2026-05-20T13:55:12Z

## Review History

### Review 1 — 2026-05-20T13:55:12Z
**Type**: Fresh
**Commit**: fb1774c60f7334b354800a0360a9baa346ca1531
**Summary**: Adds Telegram remote-control phase 1 — `/status`, `/sessions`, `/new`, `/help` commands parsed in the channel runtime command path, backed by a workspace JSON store for chat→thread bindings and busy flags. `TelegramRemoteSubscriber` on the event bus tracks in-flight turns for `/status`. Settings UI adds an informational callout and test.
**Gates**: CI unknown | Conflicts unknown | Unresolved feedback: 3 CodeRabbit majors (all marked addressed)
**Areas changed**: Rust core (telegram provider, channel routes, runtime startup, capability catalog), Frontend (TelegramConfig.tsx)
**Red flags**: `Closes #1805` on a PR that only covers 4 of 13+ acceptance criteria
**Linked issues**: #1805
**PR-Issue alignment**: MISMATCH — Issue #1805 requires `/abort`, `/detach`, permissions inline, scheduling, file attachments, workspace browsing, controller-backed ops, E2E coverage, and more. PR only delivers phase 1 (4 slash commands). `Closes #1805` will auto-close the issue prematurely.
**CodeRabbit dedup**: All 3 CodeRabbit findings (spawn_blocking for set_busy, workspace-scoped singleton, subscription handle lifetime) marked ✅ addressed — skipped in review.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: routes.rs (parse_runtime_command logic), startup.rs (subscription handle pattern)
**Dependency audit**: N/A — no new external dependencies (uuid, chrono already in use)
**Test coverage**: Good unit coverage for parser, store, subscriber, and round-trip integration tests. Test isolation concern flagged (global STORE singleton in parallel tests).
**Impact scan**: New exported types `TelegramRemoteSubscriber`, `TelegramRemoteCommand` from `providers::telegram` module. `routes.rs` command dispatch extended cleanly. No breaking changes to existing channel behavior.
**Findings**:
- [BLOCKING] `Closes #1805` is premature — 13+ acceptance criteria unresolved (abort, detach, permissions, scheduling, file attachments, controller-backed ops, E2E); will auto-close issue on merge
- [major] `remote_control.rs:103,142,224` — `with_store` (blocking disk I/O) called synchronously from async functions `build_status_response`, `build_sessions_response`, `build_new_session_response`; same pattern CodeRabbit required `spawn_blocking` for in `bus.rs`
- [major] `session_store.rs:166` — `with_store_reloads_when_workspace_changes` and sibling tests mutate process-global `STORE` singleton; racey under parallel test execution (Rust default); needs `#[serial_test::serial]` or direct struct testing
- [minor] `remote_control.rs:249` — `lookup_thread_title` calls `list_threads()` O(n) just for one title; cache title in `TelegramChatBinding` at `/new` time
**Action taken**: REQUEST_CHANGES posted
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2249#pullrequestreview-4329141954
