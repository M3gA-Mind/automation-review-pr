# PR #2487 — fix(socket): recover from stale disconnected socket to prevent permanent "Connecting..."
- **Author**: @YellowSnnowmann
- **Branch**: fix/socket-stuck-connecting-reconnect → main
- **Created**: 2026-05-22T11:08:24Z
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2487
- **Status**: changes-requested
- **Last reviewed commit**: 301bb33bcba7cd5820d3080f664686a2b6cddc98
- **Last review date**: 2026-05-22T13:27:26Z

## Review History

### Review 1 — 2026-05-22T13:27:26Z
**Type**: Fresh
**Commit**: 301bb33bcba7cd5820d3080f664686a2b6cddc98
**Summary**: Two-file change to socketService. Adds an `else` branch in `connectAsync` to clear a stale disconnected socket (same token) before the async reconnect path runs, fixing a bug where `|| this.socket` guard caused an early return and left the UI stuck at "Connecting...". Includes a regression test verifying `io()` is called twice on same-token reconnect.
**Gates**: CI pass (draft — not enforced) | Conflicts: N/A | Unresolved feedback: N/A
**Areas changed**: Frontend — app/src/services/
**Red flags**: No linked issue despite checklist requiring one; DRAFT state
**Linked issues**: None found (checklist item unchecked/unanswered)
**PR-Issue alignment**: No linked issue to verify against
**CodeRabbit dedup**: CodeRabbit skipped — draft PR detected
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: Full `socketService.ts` and `socketService.test.ts` read from upstream repo
**Dependency audit**: N/A — no new dependencies
**Test coverage**: Regression test added; covers `io()` call count but not Redux dispatch path or listener-orphaning scenario
**Impact scan**: Frontend only — socketService connection management path; no exported API/type changes
**Findings**:
- [major] `app/src/services/socketService.ts:169-170` — Stale socket abandoned via `null` assignment without calling `.disconnect()`. Socket.io internal reconnect timers on the abandoned socket can fire stale Redux dispatches (socketId, status) after the new socket is already active. Also `listenerMap` not cleared — `socketService.on()` handlers from the previous session are orphaned (not attached to new socket, but present in map), breaking `off()` semantics for those callbacks.
- [minor] `app/src/services/__tests__/socketService.test.ts:295` — Test only asserts `io()` call count; doesn't verify Redux `connecting` dispatch fires on the second call. Also starts with `disconnect()` so doesn't cover listener-orphaning scenario.
- [minor] PR description checklist item "Linked issue closed via Closes #NNN" has no corresponding ## Related section — should be marked N/A if no issue exists.
**Action taken**: REQUEST_CHANGES
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2487#pullrequestreview-4345825417
