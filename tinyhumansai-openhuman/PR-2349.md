# PR #2349 — fix(memory_tree,scripts): IMMEDIATE-tx ingest + reembed skip-persistence + Windows dev-script PATH fixes
- **Author**: @sanil-23
- **Branch**: fix/memory-tree-and-win-dev → main
- **Created**: 2026-05-20
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2349
- **Status**: changes-requested
- **Last reviewed commit**: 2b1670fe388bb6420095d5fedfbb548c249416c7
- **Last review date**: 2026-05-20T14:52:39Z

## Review History

### Review 1 — 2026-05-20T14:52:39Z
**Type**: Fresh
**Commit**: 2b1670fe388bb6420095d5fedfbb548c249416c7
**Summary**: Three bugfixes addressing production-observed failures: (1) IMMEDIATE-tx in `memory_tree::ingest::persist()` so SQLite's 15 s busy_timeout applies on lock acquisition rather than being bypassed on DEFERRED upgrade — fixes Gmail per-message ingest dropping all messages under worker contention; (2) two new `*_reembed_skipped` tombstone tables with corresponding `mark_*_reembed_skipped` helpers and `NOT EXISTS` exclusions in all 3 worklist/probe SQL sites — terminates the reembed_backfill runaway loop on orphan chunks; (3) `scripts/run-dev-win.sh` prepends pnpm's dir to PATH and deduplicates PATH before `tauri dev` — fixes Windows contributors' `'pnpm' is not recognized` failure.
**Gates**: CI pass: N/A (draft, hooks bypassed) | Conflicts pass: mergeable=true | Unresolved feedback pass: N/A (fresh)
**Areas changed**: Rust core (memory_tree ingest, store, jobs), Windows dev script
**Red flags**: No linked issue; `cargo fmt` not explicitly run; PR opened as DRAFT with tests deferred to follow-up; pre-push hook bypassed via `--no-verify`
**Linked issues**: None (changes derived from debugging session)
**PR-Issue alignment**: N/A — no linked issue
**CodeRabbit dedup**: CodeRabbit flagged SC2155 (export + command substitution masks exit code) in `run-dev-win.sh` lines 497–498 — skipped. CodeRabbit approved overall.
**Resolution actions**: None (fresh review)
**Surrounding code patterns checked**: Diff reviewed in full; function signatures and SQL schema examined in diff context; no sibling files read.
**Dependency audit**: N/A — no new external deps
**Test coverage**: IMMEDIATE-tx path untested; tombstone write/exclusion/termination path untested; existing `reembed_backfill_repopulates_then_completes` (handlers/mod.rs:1062) covers only success path — does not trigger terminal failure branches
**Impact scan**: Three SQL sites updated consistently (handler batch, runtime probe, migration probe); ON DELETE CASCADE ensures tombstone rows are cleaned up with parent rows; backward-compatible (older binaries ignore new tables); no TS/frontend changes
**Findings**:
- [major] `src/openhuman/memory/tree/jobs/handlers/mod.rs` — No test for tombstone write on terminal failure, worklist exclusion via NOT EXISTS, or has_uncovered termination when all remaining chunks are tombstoned. Critical invariant of the fix is unverified.
- [minor] `src/openhuman/memory/tree/jobs/handlers/mod.rs:709` — Summary tombstone calls use fully-qualified `crate::openhuman::memory::tree::tree_source::store::mark_summary_reembed_skipped(...)` while chunk side uses `chunk_store::` alias. Inconsistent; add a `use` import.
- [minor] `src/openhuman/memory/tree/store.rs` + `tree_source/store.rs` — No built-in path to un-tombstone a row if workspace is repaired (body files restored after path move). Follow-up admin RPC mentioned in PR but no issue filed.
**Action taken**: REQUEST_CHANGES posted to GitHub
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2349#pullrequestreview-4329625495
