# PR #2449 — fix(windows): make pnpm dev:app:win work behind TLS-inspecting proxies
- **Author**: @M3gA-Mind
- **Branch**: sync/window-upstream → main
- **Created**: 2026-05-21
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2449
- **Status**: changes-requested
- **Last reviewed commit**: 456d4ef04973425d43142a73146ed281c25f83ec
- **Last review date**: 2026-05-21T17:31:17Z

## Review History

### Review 1 — 2026-05-21T17:31:17Z
**Type**: Fresh
**Commit**: 456d4ef04973425d43142a73146ed281c25f83ec
**Summary**: PR fixes Windows dev environment in two areas: (1) swaps reqwest/tungstenite TLS backend from rustls+webpki to native-tls (schannel) on Windows so corporate TLS-inspecting proxies with private CAs don't cause UnknownIssuer errors; (2) rewrites `scripts/run-dev-win.sh` to invoke `cargo-tauri.exe` directly (skipping the fragile pnpm→cmd→tauri shim chain) and uses a hermetic `.bat` wrapper for `beforeDevCommand`. All TLS changes are platform-gated with `#[cfg(target_os = "windows")]` / `#[cfg(not(target_os = "windows"))]` pairs.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (TLS clients), Tauri shell (Cargo.lock), bash dev script
**Red flags**: `motosan-ai-oauth` added as direct dep to Tauri app in lockfile without Cargo.toml change visible in diff
**Linked issues**: Refs #2374, #2298, #2377 (partial — not Closes)
**PR-Issue alignment**: Issues describe auth/login failures; PR provides the TLS layer fix that is a prerequisite. Intentionally uses Refs not Closes as issues have broader scope. Alignment is appropriate.
**CodeRabbit dedup**: CodeRabbit flagged %TEMP% space guard on VITE_WRAPPER_WIN — already implemented in PR code. Skipped.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: `src/openhuman/config/schema/proxy.rs` (proxy client factories), `src/openhuman/integrations/client.rs` (integration client)
**Dependency audit**: `motosan-ai-oauth` v0.2.0 appears in app/src-tauri/Cargo.lock as direct dep of openhuman Tauri package — not explained in PR. Flagged.
**Test coverage**: N/A — cfg-only changes, no runtime-testable paths added. PR correctly marks test checklist N/A.
**Impact scan**: All 6 Rust HTTP client construction sites touched. TLS selection consistent except error-path `Client::new()` fallback in proxy.rs.
**Findings**:
- [major] app/src-tauri/Cargo.lock:4323 — `motosan-ai-oauth` added as direct dep of openhuman Tauri app without matching Cargo.toml change in diff; unexplained OAuth library in a TLS/devscript PR is a scope/security concern
- [major] src/api/rest.rs:88 — TLS selection pattern copy-pasted 8× across 6 files; needs extraction to shared helper (e.g. `tls_client_builder()` in `src/openhuman/tls.rs`) to prevent future drift
- [minor] src/openhuman/config/schema/proxy.rs:454 — `Client::new()` fallback in error path uses reqwest default TLS (rustls when both features compiled in), bypassing Windows native-TLS selection
- [minor] scripts/run-dev-win.sh:645 — Removed PATH dedup block without documenting the assumption change; institutional knowledge about env-block overflow lost
**Action taken**: Posted COMMENT (can't REQUEST_CHANGES on own PR)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2449#pullrequestreview-4339210993
