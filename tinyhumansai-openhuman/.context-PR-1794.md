# Intelligence Context — PR #1794

## PR Metadata
- **Title**: fix(streaming): fix SSE buffer corruption and add CJK sentence splitting
- **Author**: @Sathvik-1007
- **Branch**: (single commit) → main
- **URL**: https://github.com/tinyhumansai/openhuman/pull/1794
- **Review type**: Continuation (graycyrus previously commented with minor findings)
- **Latest commit**: 589e3c6166172439ca99ed72777424b97738b4b2
- **Commit count**: 1
- **Diff size**: 3 files, +22 -2 lines

## PR Description
- Fix erroneous post-drain buffer slice in `sse_bytes_to_chunks` that corrupted multi-line SSE buffers
- Add CJK fullwidth sentence terminators (。！？) to presentation layer's `split_sentences` so Chinese responses get proper multi-bubble segmentation

### Problem
- `compatible_stream.rs:56` had a redundant `buffer = buffer[pos + 1..].to_string()` after `buffer.drain(..=pos)` — the drain already removes the processed bytes, so the second slice corrupts the buffer on multi-line SSE payloads. For CJK content where SSE chunks may contain multiple lines, this causes data loss or panics at byte boundaries.
- `split_sentences` in `presentation.rs` only recognized Latin sentence terminators (`.!?` + space + uppercase). Chinese text using `。！？` was never split into sentences, always falling through to single-bubble delivery.

### Solution
- Removed the dead `buffer = buffer[pos + 1..].to_string()` line — `drain(..=pos)` already leaves the buffer with only the unprocessed remainder.
- Added CJK fullwidth sentence terminators (`\u{3002}`, `\u{FF01}`, `\u{FF1F}`) as split points in `split_sentences`.

### Submission Checklist
All items checked: tests pass, diff coverage N/A (1-line removal + 8-line addition), no new external deps, no breaking changes.

## Red Flags
None. Title follows conventional commit format. Small focused diff. Single commit. Clear description.

## Linked Issues + Acceptance Criteria
- **Refs #1675**: "AI repeats every Chinese response twice when asked to answer in Chinese"
  - Steps to reproduce: Ask OpenHuman to respond in Chinese, observe duplicated output
  - Expected: AI responds once in fluent Chinese
  - Actual: Entire response repeated verbatim
  - Reporter hypothesis: Could be TokenJuice compression or memory tree output gen duplicating Unicode
  - Environment: Local mode, Ollama with qwen2.5
  - Note: The PR description acknowledges this is `Refs` not `Closes` — the full duplication may also involve model-level repetition (known Ollama/qwen2.5 issue). These fixes address infrastructure-level causes only.

## PR-Issue Alignment (three-way verification)
- **Issue #1675** asks for: fix Chinese response duplication
- **PR claims**: fixes SSE buffer corruption on multi-line payloads + adds CJK sentence splitting
- **Actual code**: removes redundant buffer slice, adds CJK terminators to split_sentences, adds test
- **Alignment**: The PR correctly uses `Refs` instead of `Closes` — it addresses infrastructure-level causes (buffer corruption + missing CJK segmentation) that contribute to the issue, but acknowledges the full duplication may also be model-level. This is honest and appropriate scoping.
- **No overclaims, no underclaims, no scope drift**. The PR does exactly what it says.

## Prior Review Context (continuation)
### graycyrus COMMENTED review (2026-05-15T18:48:15Z)
Overall assessment: Clean, well-scoped PR. Buffer fix is clearly correct. CJK splitting follows existing pattern nicely. Only minor gaps.

**Inline comments posted (both minor, non-blocking):**
1. `presentation.rs:329` — No debug logging for CJK code path. Sibling code uses `tracing::debug!` with `[presentation:segment]` prefixes. Suggested adding `tracing::trace!`. Noted existing Latin path doesn't log either, so consistent but convention suggests adding.
2. `presentation_tests.rs:182` — Good happy-path test. Suggested edge cases: terminator at end of string, mixed CJK+Latin, single CJK without terminator, consecutive terminators.

**Neither comment has been addressed by the author yet.**

### coderabbitai[bot] reviews:
1. DISMISSED review (2026-05-15T07:54:45Z) on commit 49ef079
2. APPROVED review (2026-05-15T14:00:38Z) on commit 589e3c6

## Changed Files
| File | Change type | Lines |
|------|-------------|-------|
| `src/openhuman/channels/providers/presentation.rs` | Enhancement | +12 -0 |
| `src/openhuman/channels/providers/presentation_tests.rs` | Test | +9 -0 |
| `src/openhuman/providers/compatible_stream.rs` | Bug fix | +1 -2 |

## Classification + Checklist
### Areas detected: Rust core

- [x] Changes are in dedicated subdirectories (`openhuman/channels/providers/`, `openhuman/providers/`)
- [x] No standalone `*.rs` at `src/openhuman/` root
- [x] Light changes — logic in `presentation.rs` (ops-level), stream in `compatible_stream.rs`
- [x] No secrets/PII in logs
- [ ] Debug logging with grep-friendly prefixes — CJK path has no logging (minor, noted in prior review)
- [x] No new external network dependencies
- [x] No new crate dependencies

## Surrounding Code Patterns
### `src/openhuman/channels/providers/`
**Files read**: slack.rs, presentation.rs (full split_sentences context)
- **Naming**: `snake_case` functions, `PascalCase` structs, `SCREAMING_SNAKE` constants (e.g., `MIN_SEGMENT_CHARS`, `MAX_SEGMENTS`)
- **Error handling**: `anyhow::Result<T>` with `?` operator; graceful degradation with `tracing::warn!` for recoverable errors
- **Imports**: `use crate::openhuman::*` with full paths; `async_trait` for trait impls
- **Logging**: `tracing::info!` for startup, `tracing::warn!` for recoverable errors, bracket notation `[module:context]`
- **Tests**: Simple `#[test]`, `assert_eq!()`, co-located in `_tests.rs` files, regression comments, boundary testing

### `src/openhuman/providers/`
**Files read**: compatible_stream.rs context, dump utilities
- **Naming**: Same `snake_case` convention
- **Error handling**: Custom `StreamError` enum for type-safe errors (`StreamError::InvalidSse`, `StreamError::Http`)
- **Imports**: `futures_util::{stream, StreamExt}`, `std::sync::atomic`
- **Logging**: `log::warn!` and `log::debug!` in some utilities
- **Streaming**: Returns boxed stream, spawns async task with channel, buffers incomplete UTF-8

## CodeRabbit Dedup
CodeRabbit approved (2026-05-15T14:00:38Z) with no specific inline findings — just a summary of changes. No actionable items to dedup against.

**graycyrus prior review flagged:**
- `presentation.rs:329` — missing trace logging for CJK path (minor, not addressed)
- `presentation_tests.rs:182` — suggested edge case tests (minor, not addressed)

## Dependency Audit
N/A — no dependency changes.

## Test Coverage
- **New CJK splitting logic** (`presentation.rs`): Has one happy-path test added (`split_sentences_splits_on_cjk_terminators`). Test verifies all 3 CJK terminators produce 3 segments. Edge cases (end-of-string terminator, mixed CJK+Latin, consecutive terminators) not yet tested — flagged in prior review as minor.
- **Buffer fix** (`compatible_stream.rs`): 1-line removal of buggy code. PR description notes this was in a code path that previously panicked/corrupted. No new test specifically for the buffer fix, but the fix is removing dead/harmful code rather than adding new logic. The existing streaming tests should cover the corrected behavior.
- Coverage gate: With only +22 lines and existing module having 28 tests passing, coverage should be adequate.

## Impact Scan
- **`split_sentences`** is a module-internal function (called by `segment_for_delivery` within the same module). No exported signature change. No external callers affected.
- **`sse_bytes_to_chunks`** is `pub(crate)` — used within the providers module. The fix doesn't change the function signature, only corrects internal buffer handling. All existing callers continue to work correctly (and now without corruption).
- N/A — no exported signature changes.

## Full Diff
```diff
diff --git a/src/openhuman/channels/providers/presentation.rs b/src/openhuman/channels/providers/presentation.rs
index bd2cd7136..e8c9e7758 100644
--- a/src/openhuman/channels/providers/presentation.rs
+++ b/src/openhuman/channels/providers/presentation.rs
@@ -311,6 +311,7 @@ fn split_sentences(text: &str) -> Vec<String> {
         current.push(chars[i]);
         let ch = chars[i];
 
+        // Latin sentence terminators: split on ". " followed by uppercase.
         if (ch == '.' || ch == '!' || ch == '?')
             && i + 2 < chars.len()
             && chars[i + 1] == ' '
@@ -325,6 +326,17 @@ fn split_sentences(text: &str) -> Vec<String> {
             continue;
         }
 
+        // CJK sentence terminators: split after fullwidth period/exclamation/question.
+        if (ch == '\u{3002}' || ch == '\u{FF01}' || ch == '\u{FF1F}') && i + 1 < chars.len() {
+            let trimmed = current.trim().to_string();
+            if !trimmed.is_empty() {
+                parts.push(trimmed);
+            }
+            current.clear();
+            i += 1;
+            continue;
+        }
+
         i += 1;
     }
 
diff --git a/src/openhuman/channels/providers/presentation_tests.rs b/src/openhuman/channels/providers/presentation_tests.rs
index e3e1dc70b..cb04ff0ac 100644
--- a/src/openhuman/channels/providers/presentation_tests.rs
+++ b/src/openhuman/channels/providers/presentation_tests.rs
@@ -180,6 +180,15 @@ fn split_sentences_single_sentence_without_terminator() {
     assert_eq!(out.len(), 1);
 }
 
+#[test]
+fn split_sentences_splits_on_cjk_terminators() {
+    let out = split_sentences("你好世界。今天天气很好！你觉得呢？");
+    assert_eq!(out.len(), 3);
+    assert_eq!(out[0], "你好世界。");
+    assert_eq!(out[1], "今天天气很好！");
+    assert_eq!(out[2], "你觉得呢？");
+}
+
 #[test]
 fn group_sentences_single_entry_roundtrip() {
     let v: Vec<String> = vec!["Hello world".into()];
diff --git a/src/openhuman/providers/compatible_stream.rs b/src/openhuman/providers/compatible_stream.rs
index 172ebe9ce..f19c53a0b 100644
--- a/src/openhuman/providers/compatible_stream.rs
+++ b/src/openhuman/providers/compatible_stream.rs
@@ -52,8 +52,7 @@ pub(crate) fn sse_bytes_to_chunks(
 
                     // Process complete lines
                     while let Some(pos) = buffer.find('\n') {
-                        let line = buffer.drain(..=pos).collect::<String>();
-                        buffer = buffer[pos + 1..].to_string();
+                        let line: String = buffer.drain(..=pos).collect();
 
                         match parse_sse_line(&line) {
                             Ok(Some(content)) => {
```
