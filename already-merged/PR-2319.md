# PR #2319 — Quiet core-state and rewards timeout diagnostics
- **Author**: @aqilaziz (Aqil Aziz)
- **Branch**: codex/1235-quiet-core-rewards-timeouts → main
- **Created**: 2026-05-20
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2319
- **Status**: changes-requested
- **Last reviewed commit**: f531225bcaf49cac3817678670b657fea3141651
- **Last review date**: 2026-05-21T00:00:00Z

## Review History

### Review 1 — 2026-05-21T00:00:00Z
**Type**: Fresh
**Commit**: f531225bcaf49cac3817678670b657fea3141651
**Summary**: Converts unconditional `console.debug` calls in Rewards.tsx and rewardsApi.ts to namespaced `debug` logger. Limits CoreStateProvider bootstrap warnings to first failure only (debug logger still records all 5 attempts). Adds `normalizeRewardsApiError` to map transport timeouts/aborts to a stable retryable message. Tests updated throughout.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Frontend (app/src/pages, app/src/providers, app/src/services/api)
**Red flags**: None
**Linked issues**: #1235 (Closes)
**PR-Issue alignment**: All 5 acceptance criteria met at surface level. Core-state warning noise reduced; recoverable rewards error state added; diagnostics preserved via debug namespace. One structural gap in error normalization pipeline (see Findings).
**CodeRabbit dedup**: CodeRabbit left no inline comments; approved with empty body. Nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: CoreStateProvider.tsx (MAX_BOOTSTRAP_RETRIES=5, SUPPRESS_POLL_WARNING_AT=6, coreStatePollFailureDebugMessage still logs all attempts)
**Dependency audit**: `debug` ^4.4.3 already in package.json — no new dependency introduced
**Test coverage**: rewardsApi.test.ts adds timeout normalization test + normalizeRewardsApiError unit tests; CoreStateProvider.test.tsx updated to match new single-warning contract
**Impact scan**: No exported type changes, no Redux state changes, no RPC/event-bus changes. Purely diagnostic output behavior.
**Findings**:
- [major] app/src/services/api/rewardsApi.ts:171 — `getMyRewards` catch block merges transport-level throws from `apiClient.get` with the intentionally-thrown `{ success: false, error }` for `!response.success` responses. Both paths run through `normalizeRewardsApiError`, which string-matches on error messages (`includes('timed out')`, `includes('timeout')`). A backend response body containing "timeout" (e.g. "Session timeout. Please log in again.") gets silently replaced with the generic retryable message — original backend signal lost. Fix: separate the try scope to cover only the `apiClient.get` call so the `!response.success` throw escapes normalization.
**Action taken**: REQUEST_CHANGES posted with inline comment on line 171 + suggested restructuring
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2319#pullrequestreview-4331639491
