# PR #2469 — fix(tauri): forward Windows local-runtime OAuth callbacks
- **Author**: @Jessomadic (Jesse)
- **Branch**: fix/windows-oauth-local-runtime-2462-2466 → main
- **Created**: 2026-05-22
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2469
- **Status**: clean
- **Last reviewed commit**: 3f49b679d9b281698350bef4c2fa369bdafad34c
- **Last review date**: 2026-05-22T13:16:16Z

## Review History

### Review 1 — 2026-05-22T13:16:16Z
**Type**: Fresh
**Commit**: 3f49b679d9b281698350bef4c2fa369bdafad34c
**Summary**: Adds a Windows-only named-pipe bridge (`deep_link_ipc_windows.rs`) so `openhuman://` OAuth callbacks arriving as secondary-process argv are forwarded to the already-running primary before the pre-CEF mutex guard exits. Secondaries forward via `try_forward_deep_links()`, the primary binds a listener via `bind_and_listen()`, and `drain_pending_urls()` flushes any queued URLs once Tauri deep-link registration completes in `setup()`. Also fixes the Windows CI workflow (yarn → pnpm) and restores missing German i18n keys.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Tauri shell (Windows), Cargo, CI, i18n
**Red flags**: None
**Linked issues**: Closes #2462 (Windows local runtime stuck on OAuth), Closes #2466 (deep-link openhuman://auth ignored on Windows 11)
**PR-Issue alignment**: Both issues describe the same root cause (pre-CEF mutex guard drops OAuth callback URL on secondary launch). The fix directly addresses all acceptance criteria.
**CodeRabbit dedup**: CodeRabbit APPROVED with empty body — no inline comments to dedup.
**Resolution actions**: None (fresh review)
**Surrounding code patterns checked**: `lib.rs` mutex guard block, `#[cfg(windows)]` call sites, setup() deep-link registration block
**Dependency audit**: `url` crate already present (v2). Three new windows-sys features added for pipe APIs. No new external deps.
**Test coverage**: Three unit tests in the module — arg filtering, URL redaction, pipe name stability.
**Impact scan**: Module is Windows-only (`#![cfg(target_os = "windows")]`). No cross-platform callers. No exported types changed. No RPC methods or event bus shapes modified.
**Findings**:
- [minor] `app/src-tauri/src/deep_link_ipc_windows.rs:127` — `dispatch_url` acquires `live_handler` and `pending_queue` locks separately; a URL arriving between the two acquisitions during `drain_pending_urls` could be stranded in a post-drain queue. Startup-only, near-zero probability in practice.
**Action taken**: COMMENT posted — no blockers. Moved to to-be-approved.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2469#pullrequestreview-4345723950
