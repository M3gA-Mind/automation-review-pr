# PR #2352 — fix(tauri): skip single-instance plugin when D-Bus session bus is unreachable
- **Author**: @YellowSnnowmann
- **Branch**: (head) → main
- **Created**: 2026-05-20T13:51:42Z
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2352
- **Status**: clean
- **Last reviewed commit**: 37fc1c00fd230fdcecdf969da8913f2449ae2fc6
- **Last review date**: 2026-05-20T14:56:13Z

## Review History

### Review 1 — 2026-05-20T14:56:13Z
**Type**: Fresh
**Commit**: 37fc1c00fd230fdcecdf969da8913f2449ae2fc6
**Summary**: Single-file change to `app/src-tauri/src/lib.rs`. Adds a D-Bus session-bus reachability probe (`dbus_address_is_supported`, `linux_dbus_session_reachable`, `can_register_single_instance_plugin`) that gates registration of `tauri-plugin-single-instance` on Linux. When the bus is unreachable (WSL2 without WSLg, `DBUS_SESSION_BUS_ADDRESS=disabled`), the plugin is skipped with a structured `log::warn!` instead of panicking. macOS/Windows paths are byte-identical to before.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Tauri shell (`app/src-tauri/src/lib.rs`)
**Red flags**: None — targeted fix with full test coverage and a specific Sentry crash report (OPENHUMAN-TAURI-TM)
**Linked issues**: Sentry OPENHUMAN-TAURI-TM (no GitHub issue filed)
**PR-Issue alignment**: Fix precisely addresses the reported panic; Sentry issue referenced in log::warn! for traceability
**CodeRabbit dedup**: CodeRabbit left no inline findings (bare APPROVED); no dedup needed
**Resolution actions**: None
**Surrounding code patterns checked**: `check_linux_display_server` sibling function for `#[cfg]` gate conventions — consistent
**Dependency audit**: No new dependencies; probe is pure env-var + Path::exists()
**Test coverage**: 8 unit tests covering: unix/tcp/launchd/autolaunch supported, `disabled`/empty/whitespace rejected, unknown transport rejected, semicolon-separated list picks first valid entry, env-var present/absent branches, socket-fallback path
**Impact scan**: Linux-only behaviour change; macOS/Windows unaffected; plugin-ordering invariant (OPENHUMAN-TAURI-A) preserved when D-Bus available
**Findings**:
- [minor] `app/src-tauri/src/lib.rs:1682` — `dbus_address_is_supported` validates transport names syntactically but not socket reachability. A valid-but-dead address (`unix:path=/run/user/999/nonexistent`) returns `true`, registers the plugin, and the upstream `.unwrap()` can still panic with `Io(...)`. Fix deterministically covers the reported `disabled` case; watch Sentry for `Io`-variant panics post-launch.
- [minor] `app/src-tauri/src/lib.rs` (else branch in `run()`) — `log::warn!` re-reads env vars independently after the probe. The log shows `XDG_RUNTIME_DIR` value but not whether `$XDG_RUNTIME_DIR/bus` socket existed, losing the key diagnostic bit that distinguishes "dir set but no socket" from "dir unset".
**Action taken**: Posted COMMENT to GitHub; moved to to-be-approved (0 critical, 0 major)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2352#pullrequestreview-4329654659
