# PR #2480 — fix(app): normalize cloud core RPC URLs
- **Author**: @YUHAO-corn (Yuhao Chen)
- **Branch**: `yuhao/fix-remote-core-cloudflare-2467` → `main`
- **Created**: 2026-05-22
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2480
- **Status**: clean
- **Last reviewed commit**: 54ca6eb4ff23b7cf7c331bfd011527cb0459deb2
- **Last review date**: 2026-05-22T13:09:27Z

## Review History

### Review 1 — 2026-05-22T13:09:27Z
**Type**: Fresh
**Commit**: 54ca6eb4ff23b7cf7c331bfd011527cb0459deb2
**Summary**: Extends `normalizeRpcUrl` to append `/rpc` when the input URL has a root or empty path. Applies normalization consistently across `configPersistence` (read/write), `coreRpcClient` (`getCoreRpcUrl`, `testCoreRpcConnection`), `coreModeSlice` (localStorage restoration), and `BootCheckGate` picker validation. Adds `redactRpcUrlForLog` utility and switches `storeRpcUrl` logging from `console.debug` to namespaced `debug`.
**Gates**: CI pass | No conflicts | No prior M3gA-Mind feedback
**Areas changed**: Frontend (app/src utils, services, store, components)
**Red flags**: None
**Linked issues**: #2467 — Desktop Client returns 405 when connecting to self-hosted Core via Cloudflare Tunnel
**PR-Issue alignment**: Issue describes 405 when user pastes Cloudflare Tunnel base URL. PR correctly fixes by normalizing base URLs to append `/rpc`. Issue reporter's diagnosis ("GET to /rpc") was off — actual bug was POST to `/` — but fix is correct. Acceptance criteria met.
**CodeRabbit dedup**: One finding — parse before trimming trailing slashes to preserve `?next=/` and `#/` (query/hash edge case). Already addressed in commit a89c207 with regression tests. Skipped entirely.
**Resolution actions**: None (fresh review)
**Surrounding code patterns checked**: `configPersistence.ts` sibling functions (`storeRpcUrl`, `getStoredRpcUrl`, `peekStoredRpcUrl`, `isAllowedCloudRpcUrl`). `coreRpcClient.ts` `getCoreRpcUrl` and `testCoreRpcConnection`.
**Dependency audit**: N/A — no new external dependencies
**Test coverage**: Good. Regression cases added for: picker continuation with base URL, test-connection probe, `getCoreRpcUrl` with stored base URL, `coreModeSlice` localStorage restoration, `storeRpcUrl`/`getStoredRpcUrl` round-trip, `redactRpcUrlForLog` redaction.
**Impact scan**: Change to `normalizeRpcUrl` export signature is backward-compatible (return type unchanged). All callers of `testCoreRpcConnection`, `getCoreRpcUrl`, `storeRpcUrl`, `getStoredRpcUrl`, `peekStoredRpcUrl` benefit from normalization without interface change.
**Findings**:
- [minor] `coreRpcClient.test.ts`:872 — `normalizeMockRpcUrl` test stub diverges from real `normalizeRpcUrl` for query-bearing URLs; test `'in web mode normalizes a stored core base URL'` checks mock behavior not real module. Suggest `vi.importActual` approach. Same duplicate at ~line 1085.
- [minor] `configPersistence.ts`:196 — `new URL(trimmed)` called only for validation, then discarded; manual string parsing follows. Using the parsed object directly (`parsed.pathname`, `parsed.href`) would be more robust and eliminate `firstUrlSuffixIndex`. Suggested refactor included in review.
**Action taken**: Posted COMMENT. No blocking issues. PR moved to to-be-approved.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2480#pullrequestreview-4345666606
